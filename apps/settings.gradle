pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // jcenter() // Remove if not needed, it's deprecated
    }
}
rootProject.name = "AppsRoot" // Optional: Define a name for the root project
include ':VideoApp'
include ':skry-fi'
